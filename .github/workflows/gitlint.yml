name: Gitlint

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  lint-commits:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Lint commit messages
        uses: jorisroovers/gitlint-action@v0.2.1
        with:
          config: .gitlint
          target: HEAD~20..HEAD
